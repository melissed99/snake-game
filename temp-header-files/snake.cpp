// testing check snake thing

bool checkSnake(snakeLength) {}
for (int i = 0; i < snakeLength; i++) {
  int bodyX[snakeLength], bodyY[snakeLength];
  bodyX[i] = snake.x[i];
  bodyY[i] = snake.y[i];
  }
  for (int i = 0; seiobgesiogisof)
  if (bodyX[i] == snake.x[i] && appleLocation.y == snake.y[i]) {
    return False;
  }
  else {
    return True;
  }
}

// before drawing apple,
bool snakeBody = checkSnake();
while (snakeBody) {
  // draw apple
}
